# OPS map repair status

- **P0 Layers control:** repaired in the shared Atlas map. The trigger now exposes its expanded state, owns its popover, restores focus on close, and supports Escape. The map control stack has an explicit foreground layer so the control and popover remain interactive.
- **P1 camera flood:** repaired. Cameras are gated below zoom 11, queried after a 250 ms viewport debounce, and rendered only inside the current map bounds with no clustering.
- **Accessibility:** completed for the Layers trigger, close action, and layer checkboxes with visible keyboard focus.
- **Verification:** OPS type-check and production build pass. Local rendered verification is blocked at the normal Supabase authentication screen because this checkout has no test credentials. The existing OPS suite has 77 passing tests; its remaining Supabase-client suite also requires those missing credentials.

P2/P3 STREAMS/SETTINGS presentation, broad visual hierarchy, and Code Black chrome remain intentionally deferred.
